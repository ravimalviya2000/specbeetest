<?php

/**
 * @file
 * Specbeetest module for admin config.
 */

/**
 * Implements hook_theme().
 */
function specbeetest_theme($existing, $type, $theme, $path): array {
  return [
    'specbeetest_block' => [
      'variables' => [
        'adminconfig' => [],
      ],
    ],
  ];
}
